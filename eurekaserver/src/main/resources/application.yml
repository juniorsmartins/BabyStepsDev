spring:
  application:
    name: eurekaserver
  cache:
    type: none

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


ribbon:
  eureka:
    enabled: true

logging:
  file:
    path: /app/logs

