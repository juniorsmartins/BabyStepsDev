server:
  port: 8003

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    sql-script-encoding: UTF-8
    characterEncoding: UTF-8

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

